package server;

public class Server {
    // service to gain client's account information (what files he owns)

    // service to add a file

    // service to share a file

    // service to change a file content (title) and notify
}
